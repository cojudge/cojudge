Given an integer array `nums`, rotate the array to the right by `k` steps, where `k` is non-negative, and return the rotated array.

**Constraints:**

- 1 ≤ nums.length ≤ 10^5
- -2^31 ≤ nums[i] ≤ 2^31 - 1
- 0 ≤ k ≤ 10^5
