Given an array `heights` where each value is the height of a histogram bar of
width `1`, return the area of the largest rectangle that can be formed from one
or more consecutive bars.

**Constraints:**

- `1 <= heights.length <= 100000`
- `0 <= heights[i] <= 10000`
