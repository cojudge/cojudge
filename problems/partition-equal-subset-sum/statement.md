Given an array `nums` of positive integers, return `true` if you can partition the
array into two subsets whose sums are equal. Every element must belong to exactly one
of the two subsets.

**Constraints:**
- `1 <= nums.length <= 200`
- `1 <= nums[i] <= 100`
