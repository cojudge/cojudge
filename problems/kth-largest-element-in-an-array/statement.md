Given an integer array `nums` and an integer `k`, return the `k`th largest element in the array.

It is the `k`th element in descending sorted order, not the `k`th distinct element.

**Constraints:**

- `1 <= k <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`
