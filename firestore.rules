rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /shares/{shareId} {
      allow read: if true;
      allow create: if true;
      // Optional: prevent overwriting existing shares
      // allow update: if false;
      // allow delete: if false;
    }
  }
}
